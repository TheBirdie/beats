# BErkeley Advanced Transportation Simulator
## Setup instructions
### Install dependencies
- Install Maven
- Build project beats-base-classes with Maven (required)
```
cd ..
git clone https://github.com/calpath/beats-base-classes.git
cd beats-base-classes
mvn compile
```

### Prepare project
`mvn eclipse:eclipse`
